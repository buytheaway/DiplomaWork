# Optional ML dependencies - install only the backend you need.
#
# pip install -r requirements-ml.txt               # installs onnx + torch + insightface extras
# pip install -r requirements-ml-onnx.txt          # ONNX (SCRFD + ArcFace) only
# pip install -r requirements-ml-torch.txt         # Torch only
# pip install -r requirements-ml-insightface.txt   # InsightFace only

-r requirements-ml-insightface.txt
-r requirements-ml-torch.txt
