# Optional ML dependencies — install only the backend you need.
#
# pip install -r requirements-ml.txt            # installs everything
# pip install insightface onnxruntime           # InsightFace only
# pip install torch opencv-python-headless      # Torch only
# pip install onnxruntime opencv-python-headless # ONNX only

# ── InsightFace backend ──────────────────────────────────────────────
insightface
onnxruntime

# ── Torch backend ────────────────────────────────────────────────────
torch

# ── shared by Torch / ONNX / InsightFace (image decode + face detect) ─
opencv-python-headless
