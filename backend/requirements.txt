# ── core (always required) ────────────────────────────────────────────
fastapi
uvicorn[standard]
sqlalchemy>=2.0
alembic
pydantic>=2.5
pydantic-settings>=2.1
faiss-cpu
numpy
python-multipart
cryptography

# ── database drivers (pick one: postgres for Docker, sqlite built-in) ─
psycopg2-binary

# ── dev / test ────────────────────────────────────────────────────────
pytest
httpx
requests
